name: Vasya CI

on:
  push:
    branches: [main]

  pull_request:
    branches: [main]

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: Say Hello
        run: echo "Hello, Vasya!"

      - name: Show folders
        run: ls -la
  
